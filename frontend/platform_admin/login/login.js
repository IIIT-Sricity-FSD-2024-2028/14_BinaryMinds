document.getElementById('platform-login').addEventListener('submit', async function (event) { event.preventDefault(); const error = document.getElementById('error'); error.textContent = ''; try { const response = await fetch('http://localhost:3000/api/auth/login', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: document.getElementById('email').value.trim(), password: document.getElementById('password').value, role: 'platform_admin' }) }); const payload = await response.json(); if (!response.ok || payload.user?.role !== 'platform_admin') throw new Error('Invalid Platform Admin credentials.'); sessionStorage.setItem('accessToken', payload.accessToken); sessionStorage.setItem('loggedInUser', JSON.stringify({ id: payload.user.user_id, name: payload.user.full_name, email: payload.user.email, role: payload.user.role, accessToken: payload.accessToken })); window.location.href = '../index.html'; } catch (err) { error.textContent = err.message || 'Unable to sign in.'; } });
